# Sertakan di blok server {} atau location PHP. Lalu: nginx -t && nginx -s reload
# 0 = nonaktifkan batas ukuran body (Nginx tidak mengembalikan 413 karena ukuran).
client_max_body_size 0;
fastcgi_read_timeout 3600;
proxy_read_timeout 3600;
proxy_connect_timeout 3600;
proxy_send_timeout 3600;
